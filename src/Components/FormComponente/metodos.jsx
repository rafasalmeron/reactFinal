import {makeRequest} from "../../services/apiService.jsx";



const [senhaInput, setSenhaInput] = useState(false);

export const VerificaCliente =  async () => {
    try {
        if (senhaInput === clientes.senha) {
            
        }
    } catch (error) {
        
    }
}